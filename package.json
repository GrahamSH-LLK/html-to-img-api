{
  "name": "html-to-img-api",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "elysia": "latest",
    "puppeteer": "^23.0.2"
  },
  "trustedDependencies": [
    "puppeteer"
  ],
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
